import bot from '../index'

export default async function listenKeyboards (ctx) {

    switch (data) {
        // case 'reg':
        //     await ctx.reply('Выбери роль 👇', {
        //         reply_markup: consts.keys_roles
        //     })
        // break
    }
}